service: smartthings-yale-smart-alarm # NOTE: update this with your service name
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs16.x
  region: eu-west-1
  memorySize: 256

functions:
  yalealarm:
    handler: handler.handle

plugins:
  - serverless-esbuild